---
/**
 * Layout for a single blog post. Magazine header (kicker + accent bar),
 * then the Markdown/MDX body in a readable `.prose` container.
 */
import type { CollectionEntry } from 'astro:content';
import FormattedDate from '../components/FormattedDate.astro';
import TagList from '../components/TagList.astro';
import { defaultLang, t } from '../i18n/ui';
import { withBase } from '../utils/base';
import { getPostVisual } from '../utils/postVisual';
import BaseLayout from './BaseLayout.astro';
import '../styles/prose.css';

interface Props {
  post: CollectionEntry<'blog'>;
}

const { post } = Astro.props;
const { title, description, pubDate, updatedDate, tags, source } = post.data;
const { kicker, displayTitle, accentVar } = getPostVisual(title, tags, post.slug);
const publishedLabel = t('post.publishedOn', defaultLang);
const updatedLabel = t('post.updatedOn', defaultLang);
const tagsLabel = t('post.tags', defaultLang);
const sourceLabel = t('post.source', defaultLang);
const backHref = withBase('/posts/');
---

<BaseLayout title={title} description={description}>
  <article class="post" style={`--post-accent: ${accentVar};`}>
    <a href={backHref} class="back-link">← {t('nav.posts', defaultLang)}</a>
    <header class="post__header">
      <span class="post__kicker">{kicker}</span>
      <h1 class="post__title">{displayTitle}</h1>
      <div class="post__meta">
        <span class="post__date">
          {publishedLabel}：<FormattedDate date={pubDate} variant="long" />
        </span>
        {
          updatedDate && (
            <span class="post__date post__date--updated">
              · {updatedLabel}：<FormattedDate date={updatedDate} variant="long" />
            </span>
          )
        }
      </div>
    </header>

    <div class="prose">
      <slot />
    </div>

    {
      tags.length > 0 && (
        <footer class="post__footer">
          <span class="post__tags-label">{tagsLabel}：</span>
          <TagList tags={tags} />
        </footer>
      )
    }
    {
      source && (
        <p class="post__source">
          {sourceLabel}：
          <a href={source} target="_blank" rel="noopener noreferrer">{source}</a>
        </p>
      )
    }
  </article>
</BaseLayout>

<style>
  .post {
    padding-top: var(--space-8);
    padding-bottom: var(--space-12);
  }

  .back-link {
    display: inline-block;
    font-size: 0.9rem;
    color: var(--color-text-subtle);
    margin-bottom: var(--space-6);
  }

  .post__header {
    margin-bottom: var(--space-8);
    padding-bottom: var(--space-6);
    border-bottom: 4px solid var(--post-accent, var(--color-accent));
  }

  .post__kicker {
    display: block;
    font-size: 0.8rem;
    font-weight: 650;
    letter-spacing: 0.04em;
    color: var(--post-accent, var(--color-accent));
    margin-bottom: var(--space-3);
  }

  .post__title {
    font-size: clamp(2rem, 5vw, 2.75rem);
    font-weight: 800;
    line-height: 1.2;
    margin: 0 0 var(--space-4);
    letter-spacing: -0.03em;
  }

  .post__meta {
    font-size: 0.9rem;
    color: var(--color-text-subtle);
  }

  .post__date--updated {
    margin-left: var(--space-2);
  }

  .post__footer {
    margin-top: var(--space-12);
    padding-top: var(--space-4);
    border-top: 1px solid var(--color-border);
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: var(--space-2);
  }

  .post__tags-label {
    color: var(--color-text-subtle);
    font-size: 0.9rem;
  }

  .post__source {
    margin-top: var(--space-4);
    font-size: 0.85rem;
    color: var(--color-text-subtle);
    word-break: break-all;
  }
</style>
